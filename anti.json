{
    "name": "semms-ecom",
    "version": "1.0",
    "build": {
        "command": "npm install && npm run build --prefix shop-swiftly && npm install --prefix backend"
    },
    "run": {
        "command": "node backend/server.js",
        "port": 5000
    },
    "env": {
        "PORT": "5000",
        "NODE_ENV": "production"
    }
}